/*
Declaren al menos tres variables (nombre, edad, activo) usando los tipos correctos
imprime un mensaje concatenando esas variables
*/

function variables(n:string, e: number, a: boolean) {
  console.log(`Hola ${n}, tienes ${e} años y tu estado es ${a}`);
}
variables("Fernando", 25, true);